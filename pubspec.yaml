name: saidco
description: "Saidco Management System"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.11.4

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter  

  cupertino_icons: ^1.0.8
  google_fonts: ^8.0.2
  firebase_core: ^4.6.0
  cloud_firestore: ^6.2.0
  fluttertoast: ^9.0.0
  esc_pos_utils_plus: ^2.0.4
  flutter_bloc: ^9.1.1
  equatable: ^2.0.8
  get_it: ^9.2.1
  syncfusion_flutter_datepicker: ^33.1.49

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:

  uses-material-design: true

  assets:
    - assets/images/logo.png
